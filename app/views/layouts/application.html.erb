<!DOCTYPE html>
<html>
  <head>
    <title>DropboxDist</title>
    <%= csrf_meta_tags %>

    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <ul id="dist_files_dropdown" class="dropdown-content">
      <li> <%= link_to "List Files", dist_files_path %> </li>
      <li> <%= link_to "Add New File", new_dist_file_path %> </li>
    </ul>
    <nav>
      <div class="nav-wrapper">
        <a href="#" class="brand-logo">DropDistBox</a>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
          <% if current_user.present? %>
            <li>
              <a class="dropdown-button" href="#!" data-activates="dist_files_dropdown">
                Files <i class="material-icons right">arrow_drop_down</i>
              </a>
            </li>
            <li> <%= link_to "Log out", destroy_user_session_path, :method => :delete  %> </li>
          <% end %>
        </ul>
      </div>
    </nav>
    <div class="container">

      <div class="col l9">
        <%= yield %>
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
    </div>
  </body>
</html>
